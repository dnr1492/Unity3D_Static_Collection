using UnityEngine;

public class UIMain : MonoBehaviour
{
    private void Start()
    {
        ConsoleLog.Show("string");
        ConsoleLog.Show(10);
        ConsoleLog.ShowError("¿¡·¯");
    }
}
